<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="purchase_order_view_form_inherit_approval" model="ir.ui.view">
        <field name="name">purchase.order.view.form.inherit.approval</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">

            <xpath expr="//header/button[@name='action_rfq_send']" position="before">
                <button name="action_approve"
                type="object"
                class="oe_highlight"
                string="Send for Approve RFQ"
                invisible="state not in ('draft')"/>

                <button name="action_final_approve"
                type="object"
                class="oe_highlight"
                string="Approve"
                invisible="state not in ('second_approval')"
                groups="odoo_approval.group_chief_manager"/>
            </xpath>
            <xpath expr="//field[@name='state']" position="attributes">
                <attribute name="statusbar_visible">draft,sent,purchase</attribute>
            </xpath>
        </field>
    </record>
</odoo>
